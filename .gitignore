# Ignore OS generated files
.DS_Store

# Ignore all subdirectories inside assets/ (where git-synced repositories are saved)
assets/*/

# Ignore component local configuration and cache files
data/config.json
data/cache/
